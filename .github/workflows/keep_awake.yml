name: Keep Streamlit Awake

on:
  schedule:
    # Runs every 1 hour
    - cron: "0 */1 * * *"
  workflow_dispatch:  # <--- ADD THIS EXACT LINE

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s https://aip-g-1-two-stage-screening.streamlit.app/ > /dev/null
