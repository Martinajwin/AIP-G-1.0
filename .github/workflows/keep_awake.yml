name: Keep Streamlit Awake

on:
  schedule:
    # Runs every 12 hours
    - cron: "0 */1 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s https://aip-g-1-two-stage-screening.streamlit.app/ > /dev/null
