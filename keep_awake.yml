name: Keep Streamlit Awake

on:
  schedule:
    # Runs every 12 hours
    - cron: "0 */1 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s https://your-app-url.streamlit.app > /dev/null
